micronaut:
  server:
    port: 9099
endpoints:
  all:
    port: 9091
akhq:
  connections:
    docker-kafka-server:
      properties:
        bootstrap.servers: "broker:9092"
      # schema-registry:
      #   url: "http://localhost:8081"
